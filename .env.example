# Required
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Docker Compose database password (required by deploy.sh / docker compose)
DB_PASSWORD=replace_with_a_long_random_password

# Direct/local development database URL. Docker Compose builds its own DATABASE_URL.
DATABASE_URL=postgresql://bot_user:replace_password@localhost:5432/wellbot

# HUNT Telegram Mini App (optional but recommended for production)
# Must be a public HTTPS URL when Telegram opens the Mini App.
HUNT_WEBAPP_URL=
HUNT_WEB_PORT=8080

# Realtime monitor tuning
# Keep polling at 5 seconds by default; actual source/API visibility can add latency.
PARSE_INTERVAL_SECONDS=5
# Safe default for small containers. Increase only after measuring memory and source limits.
PARSE_CONCURRENCY=5
# Per-source parser timeout, in milliseconds.
PARSE_TIMEOUT_MS=9000

# Durable notification outbox
NOTIFICATION_MAX_ATTEMPTS=12
NOTIFICATION_RETENTION_DAYS=14
NOTIFICATION_BATCH_SIZE=25
NOTIFICATION_DRAIN_MS=1000

# Runtime
NODE_ENV=production
LOG_LEVEL=info
